#parse("header.vm")
<div class="container">
    <ul class="list-group">
        #foreach( $order in $allOrders)
            <li class="list-group-item">$order</li>
        #end
    </ul>
</div>
#parse("footer.vm")